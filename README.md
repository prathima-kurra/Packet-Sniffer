Packet sniffer does:
* Captures all Ethernet frames on the network interface
* Parses IP packets and extracts source & destination IP
* Detects TCP / UDP / ICMP protocol
* Prints packet information (packet length, source address, destination address, and protocol type) to terminal
